# Reproducibility

All server-specific absolute paths must be replaced with repository-relative
paths or explicit external-tool requirements. The evidence files retain their
original status labels, including `NOT_CHECKED`, `PROVENANCE_PARTIAL`, and
`PROVENANCE_MISMATCH` where applicable.
