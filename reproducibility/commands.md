# Commands

The original build used Icarus Verilog/Verilator, Yosys, OpenROAD, and Python.
The Samsung PIMSimulator C++ build and binary are intentionally not part of
this repository. Add local tool paths before running any command.

The copied benches are evidence sources; their exact include/source closure
must be checked against the selected RTL before claiming a clean reproduction.
