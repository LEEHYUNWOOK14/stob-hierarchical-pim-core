# Evidence

Evidence is copied selectively from the server snapshot. Status values such as
`NOT_CHECKED`, `PROVENANCE_PARTIAL`, and `PROVENANCE_MISMATCH` are preserved.
Evidence files do not convert a research prototype into a signoff result.
